public enum ItemType { Resource, Refine, Tech, Gear, Food }

public enum CraftingCategory { Tools, Machinery, Backpack, Guns }

public enum CraftingRequirement { CraftingTable, Smelter }

public enum CableConnectionType
{
    EnergyIn,
    EnergyOut,
}
